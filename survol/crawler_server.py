# This crawls on the server side, just by listing each script available on each entity.
# Its use is mostly for testing because it cannot connect to other machines.

# It builds an internal model similar to the other scripts.

# crawler_server.py [XID]
# By default it starts from the top, just like entity.py
#
#