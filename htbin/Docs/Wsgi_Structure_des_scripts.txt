Ce qu on va faire:

Ik faut pouvoir utiliser Wsgi et donc donner une forme commune.

Tous les scritps doivent etre importables:
- Ca resoud le probleme de la documentation: On se contentera d importer les fichiers.
- Cest plus pythonique donc plus facile a comprendre.
- Ca permet d utiliser wsgi

Ce serait peut etre bien de donner aux scripts une structure de classe.

-------------------------------------------------------
Par quoi remplacer :
import lib_entities.lib_entity_Win32_Service

Logiquement:

import ns_general.Win32_Service
ou alors (Peu importe):
from ns_general import Win32_Service

Pour lib_uris, le mettre aussi dans le namespace:

# lib_common.py
from lib_uris import *
import lib_uris

nodeSchema = lib_common.gUriGen.OracleSchemaUri(dbNam,schemaNam)

... deviendrait:

nodeSchema = ns_general.gUriGen.OracleSchemaUri(dbNam,schemaNam)

Dans ns_general.__init__.py, on mettrait tout ce qui est relatif aux classes.

On peut commencer a creer ns_general et progressivement y deplacer ce qui est relatif
aux classes de ce namespace.

Difficulte avec lib_naming.EntityArrToLabel : Il faudrait utiliser le namespace
pour choisir la bonne fonction dans le bon module.

