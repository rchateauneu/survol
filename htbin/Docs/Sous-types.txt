Les sous-classes doivent etre bien distinctes des name-spaces.

Soit l arborescence des classes reflete l heritage des sous-classes.

Soit on garde toutes les classes au meme niveau, et dans ce cas on doit
pointer vers la classe de base.

Si on utilise des sous-dossiers pour pointer ver la classe de base:
Windows n admet pas les shortcuts relatifs. Zut.
Et mklink marche pas comme on veut.
