How to install Survol:

Minimal commands:
pip install survol
cgisurvol -vb

Many features are available only if specific modules are installed.
These modules are not mandatory because Survol aims at being functional on minimal Python setup.

